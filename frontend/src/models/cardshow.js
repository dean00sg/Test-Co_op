
export class News {
    constructor(news_id, header, detail, link, imageURL) {
      this.news_id = news_id;
      this.header = header;
      this.detail = detail;
      this.link = link;
      this.imageURL = imageURL;
    }
  }
  